# Freshness report

**Scan timestamp:** 2026-08-10
**Branch / commit:** `master` @ `1a7b48f` ("Initial commit from Create Next App") — **working tree is uncommitted** for everything beyond that initial scaffold commit (all app code, Supabase migrations, and docs described here exist only in the local working tree)
**Mode:** verify-only (first-time generation — no prior `docs/` existed)

## What was scanned

- Full working-tree file listing (`find . -type f`, excluding `node_modules/`, `.git/`, `.next/`, `.claude/`, `.agents/`)
- `git status --short` (19 changed/untracked top-level entries) and `git log --oneline` (1 commit total)
- `package.json` (dependencies, scripts)
- `components.json`, `next.config.ts`, `.env.local.example`
- All 12 files in `supabase/migrations/` + `supabase/seed.sql` + `supabase/config.toml`
- Every file under `src/app/`, `src/components/`, `src/lib/`, `src/styles/`, `src/types/`
- Live Supabase project state (via `supabase db advisors --linked`, direct `db query` calls, and REST API smoke tests) — used to confirm which schema features are actually deployed and working vs. only defined in migration files not yet pushed (as of this scan, all 12 migrations are pushed and confirmed live)

## Existing docs — status

| Doc | Last modified | Status | Notes |
|---|---|---|---|
| `README.md` | initial commit | current (but generic) | Default `create-next-app` boilerplate — describes `next dev`/`next build`, nothing project-specific. Not rewritten by this scan (verify-only mode; rewriting `README.md` needs explicit permission). |
| `AGENTS.md` | initial commit | current | Contains the "This is NOT the Next.js you know" instructions pointing agents at `node_modules/next/dist/docs/` before writing Next.js code. Auto-regenerated by `next dev` per its own header comment — do not hand-edit; removing it from a diff just re-creates the uncommitted change. |
| `CLAUDE.md` | initial commit | current | One line: `@AGENTS.md` (import directive). |
| `docs/*` | did not exist before this scan | n/a | All 8 canonical docs generated fresh in this pass. |

No pre-existing doc was found to be stale, because no project-specific docs existed before this scan — everything in `docs/` is new.

## Stale or missing docs

- **Missing:** everything in `docs/` (now created).
- **Not stale, but incomplete by design:** `README.md` still reflects `create-next-app` defaults rather than this project. Left untouched per verify-only mode — flagging for the user to decide whether to update it.

## Code changes since last scan

N/A — this is the first scan. For context, everything except the single initial commit is new/uncommitted:

- Full theming system (`src/styles/themes/`, `src/app/globals.css`, `src/components/theme/`)
- 7-primitive motion library (`src/components/motion/`)
- Full marketing homepage (`src/components/marketing/`, `src/app/(marketing)/`)
- 12 Supabase migrations (schema, RLS, triggers, 2 RPCs) — pushed and live
- Auth system (Server Actions, forms, guards, middleware)
- Patient registration flow (Digital Registration module)
- Staff and portal shells + stub pages for unbuilt modules
- Two third-party agent-tooling directories: `.claude/skills/{supabase,supabase-postgres-best-practices}` and `.agents/skills/{supabase,supabase-postgres-best-practices}` (installed via the `supabase` skill's own setup, tracked in `skills-lock.json`)

## What was changed

(Verify-only mode — this section lists what was *written*, since no docs existed to edit.)

- Created `docs/PROJECT_MAP.md`
- Created `docs/MODULE_INDEX.md`
- Created `docs/CONTRACTS.md`
- Created `docs/DATA_DICTIONARY.md`
- Created `docs/GLOSSARY.md`
- Created `docs/TEST_LANDSCAPE.md`
- Created `docs/FRESHNESS_REPORT.md` (this file)
- Created `docs/index.md`

No application code, migrations, or existing top-level docs (`README.md`, `AGENTS.md`, `CLAUDE.md`) were modified.

## Recommended next actions

1. **Commit.** Nothing described in these docs is in git history yet beyond the initial scaffold — a crash or lost working tree would currently lose all of it (this already happened once this session; the work survived because it was on disk, not because it was committed).
2. Update `README.md` to describe the actual project (currently generic `create-next-app` boilerplate) — requires explicit go-ahead per this skill's rules.
3. Re-run this skill (or at least regenerate `FRESHNESS_REPORT.md`) after each of the remaining modules (scheduler, booking, invoicing, prescriptions, odontogram, reminders) ships, since `MODULE_INDEX.md` and `CONTRACTS.md` currently document several routes as `PLANNED`/stub.
4. Consider wiring an automated test suite (see `docs/TEST_LANDSCAPE.md` "Recommended next actions") before the freshness of these docs starts depending on memory alone.

## Update — 2026-08-10 (later same day)

All 4 recommended actions above from the original scan are now stale in a good way:

- **Action 1 (commit) — done.** Every module below now has its own commit; `git log --oneline` shows 12 commits beyond the initial scaffold.
- **Action 3 (remaining modules) — done.** All 9 Basic-tier modules are built and live-verified: Scheduler (3), Online Booking (4) — with a real RLS gap found and fixed (`appointments_self_update` replaced by `cancel_appointment` RPC) — Invoice Management (6), Prescription Documentation (7), Dental Odontogram (8), Confirmation & Reminders (9, trigger + pg_cron based, dispatch layer only — no email/SMS provider credentials in this environment). Marketing pages (`/about`, `/services[+slug]`, `/practitioners[+id]`, `/contact`) are built against real Supabase data, not mock data — this surfaced and fixed a second gap (`profiles` had no public read policy, so practitioner names silently failed to render for signed-out visitors).
- **A hardening pass** re-ran `supabase db advisors`, found two functions (`queue_appointment_reminders`, `handle_new_user`) callable by anon/authenticated despite an earlier `revoke ... from public`, and fixed it with explicit role revokes (verified live: signup trigger still works, direct anon RPC call now correctly denied). `npm run lint` is clean (was 8 errors).
- **Action 2 (README) — still not done**, still requires explicit user go-ahead.
- **Action 4 (test suite) — still not done.** This remains the single biggest structural gap: every verification in this project has been a manual, one-off `curl` session against the live database, not a repeatable automated test.
- **Explicitly out of scope, needs the user:** production deployment (hosting target/credentials not provided — see `docs/PROJECT_MAP.md`'s subpath-of-effytechbd.com context), email/SMS provider wiring for notification dispatch, and the `multiple_permissive_policies` RLS performance optimization flagged by `db advisors` (deliberately deferred, documented in `docs/TEST_LANDSCAPE.md`, not a correctness issue).
